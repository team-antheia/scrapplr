import { combineReducers, createStore } from 'redux';

export default createStore(
  combineReducers({
    

  
    currentPage: {

    }


  })
);


// to get page, we need scrapbookId 


// upload photo, goes directly to DB, and then when component rerenders, updated data is also pulled down 